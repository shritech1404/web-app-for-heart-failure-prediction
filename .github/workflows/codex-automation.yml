name: Codex Automated Review

on:
  pull_request:
    branches:
      - codexNew
    types:
      - opened
      - synchronize
      - reopened

jobs:
  codex-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run lint / style checks
        run: |
          if [ -x ./codex ]; then
            ./codex lint
          else
            echo "codex CLI not found; skipping lint checks."
          fi
        # this will stop if exit code ≠ 0

      - name: Check vulnerabilities / dependencies
        run: |
          if [ -x ./codex ]; then
            ./codex scan vulnerabilities
          else
            echo "codex CLI not found; skipping vulnerability scan."
          fi
        # fails if vulnerabilities found

      - name: Ensure README / docs updated
        run: |
          if [ -x ./codex ]; then
            ./codex check-readme
          else
            echo "codex CLI not found; skipping README/docs check."
          fi
        # fail if checks not satisfied

      - name: Post summary comment
        if: always()
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ${{ job.status == 'success' && '✅ All checks passed' || '❌ Some checks failed' }}
